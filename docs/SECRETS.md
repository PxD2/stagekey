# Secrets stay off this repo

Do not commit keys, tokens, passwords, auth files, or account login material.

Sign in on the machine (`grok login`, Adversal browser sign-in). Those files live in the home directory, not in Git.

If something that looks like a credential is ever pasted into a file here, delete it, rotate it, and do not push it.
